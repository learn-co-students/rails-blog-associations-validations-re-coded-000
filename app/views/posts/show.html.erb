<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
</p>
<p>
  <h3>Tags:</h3>
  <ul>
  <%@post.tag_ids.each do |tag_id|%>
    <li><%= Tag.find(tag_id).name %></li>

  <% end %>
</ul>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
